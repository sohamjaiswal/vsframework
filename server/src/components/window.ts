export class Window {
    resX: number
    resY: number
    screens: number
    constructor(resX: number, resY: number, screens: number) {
        this.resX = resX;
        this.resY = resY
        this.screens = screens
    }
}